import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Color Coordination Guide — What Colors Go Together for Outfits | MurmReps",
  description: "Interactive color matching guide. Learn which colors pair well for streetwear, old money, and everyday outfits. 12 colors, 10 outfit formulas, seasonal palettes.",
};

export default function Layout({ children }: { children: React.ReactNode }) { return children; }
